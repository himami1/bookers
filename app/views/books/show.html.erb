<body>
   <% if flash[:notice] %>
  <p class="alert alert-success"><%= notice %></p>
   <% end %>
  
  <div><p><strong>Title:</strong> <%= @book.title %> </p></div>
  
  <div><p><strong>Body:</strong> <%= @book.body %> </p></div>
  
  <%= link_to 'Edit', edit_book_path(@book.id) %>
   | 
  <%= link_to 'Back', "/books" %>
</body>
